---
name: Release

on:
  push:
    tags:
      - 'v[0-9]+.*'
      - 'ngdp-client-v[0-9]+.*'

  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 0.1.0)'
        required: true
        type: string
      release-type:
        description: 'Release type'
        required: true
        type: choice
        options:
          - all
          - libraries
          - cli
        default: cli
      dry-run:
        description: 'Dry run (no actual publishing)'
        required: false
        type: boolean
        default: false
      skip-tests:
        description: 'Skip tests (use with caution)'
        required: false
        type: boolean
        default: false

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUSTFLAGS: -D warnings
  CARGO_INCREMENTAL: 0

# Prevent concurrent releases
concurrency:
  group: release-${{ github.repository }}
  cancel-in-progress: false

permissions:
  contents: write
  packages: write

jobs:
  determine-release-type:
    name: Determine Release Type
    runs-on: ubuntu-latest
    outputs:
      release-libraries: ${{ steps.determine.outputs.release-libraries }}
      release-cli: ${{ steps.determine.outputs.release-cli }}
      version: ${{ steps.determine.outputs.version }}
      dry-run: ${{ steps.determine.outputs.dry-run }}
      skip-tests: ${{ steps.determine.outputs.skip-tests }}
    steps:
      - name: Determine what to release
        id: determine
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            VERSION="${{ inputs.version }}"
            RELEASE_TYPE="${{ inputs.release-type }}"
            DRY_RUN="${{ inputs.dry-run }}"
            SKIP_TESTS="${{ inputs.skip-tests }}"
          else
            # Parse tag to determine release type
            TAG="${GITHUB_REF#refs/tags/}"
            if [[ "$TAG" == ngdp-client-v* ]]; then
              VERSION="${TAG#ngdp-client-v}"
              RELEASE_TYPE="cli"
            elif [[ "$TAG" == libs-v* ]]; then
              VERSION="${TAG#libs-v}"
              RELEASE_TYPE="libraries"
            else
              VERSION="${TAG#v}"
              RELEASE_TYPE="all"
            fi
            DRY_RUN="false"
            SKIP_TESTS="false"
          fi

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "dry-run=$DRY_RUN" >> $GITHUB_OUTPUT
          echo "skip-tests=$SKIP_TESTS" >> $GITHUB_OUTPUT

          # Determine what to release
          case "$RELEASE_TYPE" in
            all)
              echo "release-libraries=true" >> $GITHUB_OUTPUT
              echo "release-cli=true" >> $GITHUB_OUTPUT
              echo "::notice::Releasing both libraries and CLI for v$VERSION"
              ;;
            libraries)
              echo "release-libraries=true" >> $GITHUB_OUTPUT
              echo "release-cli=false" >> $GITHUB_OUTPUT
              echo "::notice::Releasing libraries only for v$VERSION"
              ;;
            cli)
              echo "release-libraries=false" >> $GITHUB_OUTPUT
              echo "release-cli=true" >> $GITHUB_OUTPUT
              echo "::notice::Releasing CLI only for v$VERSION"
              ;;
          esac
          
          if [[ "$DRY_RUN" == "true" ]]; then
            echo "::warning::DRY RUN MODE - No actual publishing will occur"
          fi

  # Release libraries to crates.io
  release-libraries:
    name: Release Libraries
    needs: determine-release-type
    if: needs.determine-release-type.outputs.release-libraries == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Verify versions match
        run: |
          VERSION="${{ needs.determine-release-type.outputs.version }}"
          for crate in ngdp-bpsv ribbit-client tact-client tact-parser ngdp-cdn ngdp-cache ngdp-crypto blte ngdp-client; do
            CRATE_VERSION=$(cargo metadata --format-version 1 --no-deps | jq -r ".packages[] | select(.name == \"$crate\") | .version")
            if [[ "$CRATE_VERSION" != "$VERSION" ]]; then
              echo "Version mismatch for $crate: expected $VERSION, found $CRATE_VERSION"
              exit 1
            fi
          done

      - name: Run tests
        if: needs.determine-release-type.outputs.skip-tests != 'true'
        run: cargo test --all-features --workspace

      - name: Publish to crates.io (Dry Run)
        if: needs.determine-release-type.outputs.dry-run == 'true'
        run: |
          echo "::warning::DRY RUN MODE - Would publish these packages in dependency order:"
          echo "Level 0 (no internal deps):"
          echo "  1. ngdp-bpsv"
          echo "  2. ngdp-crypto"
          echo "Level 1 (depends on Level 0):"
          echo "  3. ribbit-client (→ ngdp-bpsv)"
          echo "  4. tact-client (→ ngdp-bpsv)"
          echo "  5. blte (→ ngdp-crypto)"
          echo "Level 2 (depends on Level 1):"
          echo "  6. tact-parser (→ blte, ngdp-crypto)"
          echo "  7. ngdp-cdn (→ tact-client)"
          echo "Level 3 (depends on Level 2):"
          echo "  8. ngdp-cache (→ ngdp-cdn, ribbit-client, tact-client)"
          echo "Level 4 (depends on all):"
          echo "  9. ngdp-client (→ all crates)"
          echo "::notice::No actual publishing performed"
          
      - name: Publish to crates.io (Production)
        if: needs.determine-release-type.outputs.dry-run != 'true'
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
        run: |
          echo "::notice::Publishing packages to crates.io in dependency order..."
          
          # Level 0: No internal dependencies
          echo "::group::Level 0 - Base crates (no internal deps)"
          cargo publish -p ngdp-bpsv
          cargo publish -p ngdp-crypto
          echo "::endgroup::"
          sleep 45  # Longer wait for base crates to be indexed
          
          # Level 1: Depend on Level 0 only
          echo "::group::Level 1 - First level dependencies"
          cargo publish -p ribbit-client    # → ngdp-bpsv
          cargo publish -p tact-client      # → ngdp-bpsv  
          cargo publish -p blte             # → ngdp-crypto
          echo "::endgroup::"
          sleep 45  # Wait for Level 1 to be indexed
          
          # Level 2: Depend on Level 1
          echo "::group::Level 2 - Second level dependencies"
          cargo publish -p tact-parser      # → blte, ngdp-crypto
          cargo publish -p ngdp-cdn         # → tact-client
          echo "::endgroup::"
          sleep 45  # Wait for Level 2 to be indexed
          
          # Level 3: Depend on Level 2
          echo "::group::Level 3 - Third level dependencies"
          cargo publish -p ngdp-cache       # → ngdp-cdn, ribbit-client, tact-client
          echo "::endgroup::"
          sleep 45  # Wait for Level 3 to be indexed
          
          # Level 4: CLI depends on everything
          echo "::group::Level 4 - Final CLI crate"
          cargo publish -p ngdp-client      # → all crates
          echo "::endgroup::"
          
          echo "::notice::All packages published successfully in correct dependency order!"

      - name: Create library release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.determine-release-type.outputs.version }}
          name: cascette-rs v${{ needs.determine-release-type.outputs.version }}
          body: |
            ## Library Release v${{ needs.determine-release-type.outputs.version }}

            This release includes updates to all cascette-rs libraries:
            - ngdp-bpsv
            - ribbit-client
            - tact-client
            - tact-parser
            - ngdp-crypto
            - blte
            - ngdp-cdn
            - ngdp-cache
            - ngdp-client

            ### Installation

            Add to your `Cargo.toml`:
            ```toml
            [dependencies]
            ngdp-bpsv = "${{ needs.determine-release-type.outputs.version }}"
            ribbit-client = "${{ needs.determine-release-type.outputs.version }}"
            # ... other crates as needed
            ```

            See the [CHANGELOG](https://github.com/wowemulation-dev/cascette-rs/blob/main/CHANGELOG.md) for details.

  # Release CLI binaries
  release-cli:
    name: Release CLI
    needs: determine-release-type
    if: needs.determine-release-type.outputs.release-cli == 'true'
    uses: ./.github/workflows/release-cli.yml
    with:
      version: ${{ needs.determine-release-type.outputs.version }}
      dry-run: ${{ needs.determine-release-type.outputs.dry-run == 'true' }}
    secrets: inherit

  # Final success check
  release-success:
    name: Release Success
    if: always()
    needs: [determine-release-type, release-libraries, release-cli]
    runs-on: ubuntu-latest
    steps:
      - name: Check release status
        run: |
          echo "Release type determination: ${{ needs.determine-release-type.result }}"
          echo "Library release: ${{ needs.release-libraries.result }}"
          echo "CLI release: ${{ needs.release-cli.result }}"
          
          # Check if any required job failed
          if [[ "${{ needs.determine-release-type.outputs.release-libraries }}" == "true" ]] && [[ "${{ needs.release-libraries.result }}" != "success" ]]; then
            echo "Library release failed"
            exit 1
          fi
          
          if [[ "${{ needs.determine-release-type.outputs.release-cli }}" == "true" ]] && [[ "${{ needs.release-cli.result }}" != "success" ]]; then
            echo "CLI release failed"
            exit 1
          fi
          
          echo "All releases completed successfully!"